# 花藤专注（VineFocus）项目交接文档

> 交接版本：v1.7.3<br>
> 文档日期：2026-09-15<br>
> 支持平台：Windows 10/11、macOS<br>
> 技术栈：Python 3.10+、PySide6 6.5+

唯一正式工作目录：`D:\Projects\VineFocus`  
GitHub：`https://github.com/flyallz/VineFocus`

后续修改、测试、`git status`、提交与推送都只围绕此目录进行；桌面 `flower`、`D:\Codex` 及历史导出副本均不是开发源。

## 1. 项目概况

花藤专注是一款桌面专注计时工具。它把专注过程映射为自然植物成长：每轮专注都会推动枝叶生长，用户在结束后确认“本轮目标有推进”时，植物会进入更丰富的开花阶段。花朵数量与形态由植物习性和构图决定，不与记录数机械对应。

产品核心不是用植物遮满屏幕，而是让植物安静地出现在桌面边缘，在持续专注后逐渐形成一株具有个人积累感的花藤。

v1.7.3 已完成以下闭环：

- 番茄钟、暂停、继续、重置、休息、轮次循环。
- 场景、任务、本轮目标与自定义任务。
- 专注结束后的成果记录，以及“目标有推进 / 只是保持了专注”选择。
- 累计成长、自然开花阶段、24 个成长单元换株。
- 四套植物、三种构图、连续枝叶密度、独立尺寸和透明度控制。
- 夜色流光、温室晨雾、薄荷汽水三套界面。
- 主面板、迷你计时器、系统托盘/菜单栏和成长看板。
- 本地 JSON、每日 Markdown、滚动备份和会话快照。
- Windows 开机启动、前台全屏检测；macOS 登录项。
- 单实例运行、托盘恢复、多显示器目标和屏幕断开回收。
- 用户明确选择本轮结果后才会推进开花；可选成果数据独立汇总，不参与生长。

## 2. 本次版本重点

### 2.1 器官连接修正

52 个植物器官不再共用“底部中央”连接点：

- 普通叶片从实际叶柄位置连接。
- 枝条从木质切口处连接，由主藤覆盖根部切面。
- 花苞和普通花朵从花梗连接。
- 紫藤复叶及花序从上方枝柄连接，并按悬垂方向旋转。
- 极光心形叶从顶部凹口处连接，并顺重力向下展开。
- 镜像时同步修正局部生长轴，避免翻转后再次错位。

对应实现位于 `vinefocus/botanical_assets.py` 的 `ORGAN_TRANSFORMS`、`WISTERIA_TRANSFORMS`、`AURORA_TRANSFORMS` 和 `BotanicalAssets.draw_organ()`。

### 2.2 自然收梢

每段已完成路径都会在末端生成嫩叶组合；标准和茂盛档有第二片端叶，樱雾和极光的茂盛档可出现末端花苞。主藤不再以裸露圆头结束。

对应实现位于 `vinefocus/overlay.py` 的 `draw_terminal_caps()`。

### 2.3 连续密度与独立存在感

| 数值区间 | 语义 | 构图意图 |
|---|---|---|
| 0～33 | 清爽 | 大量留白、最低干扰 |
| 34～69 | 标准 | 日常自然节奏 |
| 70～100 | 茂盛 | 更多分枝、叶片与花位 |

密度只改变节点数量、成对叶概率和可用花位。植物尺寸、植物透明度各自使用独立滑动条，互不偷改。

### 2.4 贴边攀援与自然开花

- 侧边攀援主藤限制在约 16～72 个逻辑像素的边缘带，高分屏不再内缩。
- 目标有推进时推进开花阶段，但不向用户显示“花簇数量”。
- 月白、樱雾和极光由同一枝点自然展开多朵花，紫藤使用完整花序；这些只是内部绘制结构。
- 环屏四段共享 0～100% 成长时间轴；每株先稳定随机区域顺序，再按轮转开放，前四次推进必然覆盖四段且都位于已生长范围。
- 每次推进只增加一个内部花位，但该花位按主题表现为多朵组合（樱雾 4 朵、月白/极光 3 朵）或完整紫藤花序；新花用约 1.2 秒从苞舒展到盛开。
- 未获得的内部花位完全不绘制，不再伪装为“待开花苞”；普通生长花苞由独立节点承担。
- 休息呼吸由独立低频计时器驱动，休息尚未开始倒计时时也能看见；花朵约 5.5% 缩放，减少动效时完全停止。

### 2.5 本轮界面与稳定性修正

- 极光直属心形叶只朝内容区生长，取消同锚点反向复制；枝、苞、花附近采用更大净距。
- 主面板固定为内容高度，任务编辑器开合后重新计算，不能由用户或旧窗口状态拉出底部空白。
- 主面板两处植物预览不再强制至少显示 55%/68% 成熟度，而是跟随覆盖层实时进度。
- 托盘菜单在 Windows/Linux 使用独立主题样式，同时设置文字与背景；macOS 使用原生菜单。
- 满 24 轮的植物会保持完整展示，下一次点击开始时先换株和种子，再启动计时。

## 3. 项目目录

```text
VineFocus/
├── assets/
│   └── botanical/
│       ├── organs/                 # 四植物 × 13 类透明器官
│       ├── moon_base.png           # 卡片/看板兼容植物底图
│       ├── cherry_base.png
│       ├── wisteria_base.png
│       ├── aurora_base.png
│       └── flower_atlas.png
├── previews/                       # 离屏视觉验收图，不参与运行
├── tests/
│   └── test_smoke.py               # 无界面回归测试
├── vinefocus/
│   ├── __init__.py                 # 版本号
│   ├── config.py                   # 场景、主题、布局常量
│   ├── models.py                   # VineNode 等跨模块模型
│   ├── state.py                    # 设置、记录、成长、备份
│   ├── styles.py                   # 三套 UI 令牌和 QSS
│   ├── visual_profiles.py          # 三类生长路径和植物比例
│   ├── botanical_assets.py         # 资源、锚点、变换和卡片植物
│   ├── overlay.py                  # 全屏花藤与成长渲染
│   ├── widgets.py                  # Toast、统计卡、迷你计时器
│   ├── dialogs.py                  # 产出窗口和成长看板
│   ├── settings_dialog.py          # 三页设置窗口
│   ├── panel.py                    # 计时状态机和主面板
│   ├── tray.py                     # 托盘/菜单栏控制
│   ├── single_instance.py          # 单实例和已有窗口唤醒
│   └── platform_integration.py     # Windows/macOS 系统集成
├── main.py                         # 唯一应用入口
├── pyproject.toml                  # 版本和依赖
├── requirements.txt
├── README.md
├── IMPLEMENTATION_NOTES.md
├── 花藤专注_项目交接文档_v1.7.3.md
├── CHANGELOG.md
├── 发布前审查报告_v1.7.3.md
└── 花藤专注_完整项目需求说明书.md
```

所有手写 Python 文件顶部均有职责注释。业务代码与植物绘制已经分离，不应重新合并成单文件。

## 4. 运行方式

### Windows

```powershell
cd D:\Projects\VineFocus
python -m pip install -r requirements.txt
python main.py
```

### macOS

```bash
cd /path/to/VineFocus
python3 -m pip install -r requirements.txt
python3 main.py
```

若运行新版本后没有新窗口，先在旧版本托盘菜单中选择“退出软件”。所有版本使用同一单实例标识，已有进程会拦截第二次启动并唤醒旧窗口。

## 5. 核心业务逻辑

### 5.1 计时状态

```text
准备专注 → 专注中 ⇄ 已暂停 → 等待记录产出
                              ↓
                    进入休息 → 休息结束
                              ↓
                   下一轮准备 / 全部完成
```

- 运行时使用单调时钟计算经过时间，避免界面卡顿造成累计误差。
- 未完成会话每 5 秒保存一次。
- 系统长时间挂起后恢复会自动暂停，不把离线时间计入专注。
- 休息结束不会擅自开始下一轮。
- 关闭产出记录窗口后保持“待记录”，不会绕过产出逻辑。

### 5.2 成长逻辑

- 当前章节默认包含 24 个完成单元。
- 一次正常完成的专注增加 1 个完成单元。
- “本轮目标有推进”同时增加 1 个推进单元，并推进自然开花阶段。
- “只是保持了专注”仍保存记录并增加枝叶；不会产生虚构的“待开花苞”。
- 数据层继续使用旧字段 `effective` 保持历史兼容，但所有用户可见文案使用“目标有推进”。
- 第 24 个单元完成后保留完整成品；用户下一次点击“开始”时先进入下一株、清零当前章节并切换稳定种子，再启动计时。
- “单轮成长”只展示本轮；“累计成长”展示当前章节累积。

### 5.3 成长看板的定位

成长看板是专注成果与植物成长的统计页，不承担计时控制。当前包含：

- 当前植物及成长进度。
- 专注轮次、累计专注分钟、连续专注天数、目标推进次数。
- 当前植株卡只读取当前 24 轮章节；今日/7天/30天筛选只影响历史统计，两套口径不混用。
- 最近 7 天趋势和任务类型分布。
- 最近产出卡片。
- 复制摘要、打开记录目录。

看板功能保留。未来修改时不得把它变成另一个设置页或重复计时器。

## 6. 数据与兼容

默认数据目录：

```text
~/.vine_pomodoro/
├── settings.json
├── active_session.json
└── records/
    ├── records.json
    ├── records.json.bak1
    ├── records.json.bak2
    ├── records.json.bak3
    └── YYYY-MM-DD_花藤专注.md
```

- `settings.json` 保存设置、今日计数和当前成长章节。
- `records.json` 最多保留最近 3000 条结构化记录。
- 每日 Markdown 便于用户直接阅读和二次整理。
- JSON 使用临时文件替换，记录保存前保留三份滚动备份。
- v12 及更早版本的植物主题名称由 `THEME_ALIASES` 自动迁移。
- 不上传数据，不依赖账号，不要求云服务。

## 7. 视觉系统维护规则

### 7.1 植物资源

- `assets/botanical/organs/` 必须随包分发。
- 不要重新截取整株图代替器官生长。
- 新增器官时同时补充锚点、局部轴和透明边缘测试。
- 主藤、侧枝生长和节点排序由 QPainter 控制；叶、花苞和花朵使用透明 PNG。
- 绘制顺序保持：侧枝/卷须 → 主藤封口 → 端叶/直属叶/花苞/花 → 收梢。
- 4K/Retina 尺寸缩放上限保持 1.35，除非完成完整 DPI 回归。

### 7.2 UI

- 夜色流光是默认主题，强调低饱和石墨蓝、雾面卡片、柔和青绿主操作。
- 温室晨雾和薄荷汽水继续保留。
- 当前“玻璃效果”是跨平台半透明、渐变和投影模拟，不调用 Windows Acrylic 或 macOS vibrancy 私有接口。
- 真实系统模糊若以后加入，必须提供失败回退，并分别真机测试。

## 8. 自动测试

```powershell
python -m compileall -q .
python -m unittest discover -s tests -v
```

测试覆盖：

- 四植物 × 三布局 × 多阶段渲染。
- 52 个器官加载、Alpha 通道和透明角。
- 器官锚点语义。
- 低、中、高代表密度的节点数递增，连续滑块可完整调节。
- 稳定伪随机和高 DPI 上限。
- 环屏贴边、不对称构图、四段同步成熟、前四个开花事件真实可见。
- 极光叶片朝向与共锚去重、主面板内容高度、卡片进度一致性、托盘菜单明暗对比。
- 历史主题迁移、成长规则和会话快照。
- 单实例、主面板/迷你模式和托盘恢复。

## 9. 打包

### Windows

```powershell
pyinstaller --noconsole --onedir --name VineFocus --add-data "assets;assets" main.py
pyinstaller --noconsole --onefile --name VineFocus --add-data "assets;assets" main.py
```

优先验收 `--onedir`，确认资源完整后再生成单文件。对外分发需要可信代码签名，否则 Windows Smart App Control/SmartScreen 可能拦截。

### macOS

```bash
pyinstaller --windowed --onedir --name VineFocus --add-data "assets:assets" main.py
```

macOS `.app` 必须在真实 Mac 上构建、签名、公证并验证 Gatekeeper。Windows 环境不能代替 Mac 真机验收。

## 10. 真机回归清单

- Windows 100%、125%、150%、200% 缩放。
- Retina 与非 Retina、不同 DPI 双屏、拔插外接屏。
- 四植物 × 三布局 × 低/中/高代表密度 × 明暗桌面。
- 路径末端有自然收梢，无裸露切口。
- 叶柄、花梗、紫藤复叶及花序均连接到枝条。
- 托盘隐藏/恢复连续 50 次，无尺寸、位置或透明度漂移。
- 迷你圆点悬停展开、暂停、恢复和主面板切换。
- 专注、休息、暂停、继续、重置、跳过休息、最终轮。
- 目标推进后有一个可见的主题花组并跨区域分布；只保持专注时枝叶成长仍被保留。
- 成果数据默认折叠、可选，并且不改变花朵数量。
- 休息状态的花朵呼吸和枝叶轻摆肉眼可见；开启减少动效后静止。
- 睡眠/锁屏恢复后自动暂停。
- Windows 全屏应用时花藤隐藏并在退出后恢复。
- macOS 菜单栏、多个 Space、Command+Q、登录项和通知权限。

## 11. 已知边界与后续建议

- 当前植物资产来自统一概念稿拆分，已修正锚点，但仍需在真实 4K 桌面逐主题微调 1～3 像素级连接位置。
- 当前玻璃是跨平台模拟，不包含实时桌面模糊。
- 当前成长章节固定为 24 次，尚未提供用户自定义章节长度。
- 当前记录为本地文件，尚未实现云同步、多设备或账号系统。
- 优先后续：真机视觉验收 → 无障碍键盘导航 → 签名与安装包 → 可选的成长回顾导出。

## 12. 交付判定

接手者在满足以下条件后方可宣告发布候选版：

1. 全量编译及自动测试通过。
2. Windows 与 macOS 真机核心路径通过。
3. 连续密度调节平滑，低/中/高差异清晰且均不遮挡主要工作区。
4. 叶、花、枝连接无明显悬空，所有路径自然收梢。
5. 旧设置和历史记录可以读取。
6. 托盘恢复、单实例和退出逻辑无回归。
7. 安装包包含全部植物资源并完成平台签名。
