# 花藤专注（VineFocus）项目交接文档 v1.7.4

> 更新日期：2026-09-15  
> 唯一正式工作目录：`D:\Projects\VineFocus`  
> GitHub：<https://github.com/flyallz/VineFocus>  
> 目标分支：`main`

## 1. 交接结论

v1.7.4 已完成任务轮数驱动的桌面藤上花期、三种环屏节奏、下一轮连续生长、结果语义纠偏、休息呼吸增强、标题按钮统一和高密度器官避让。当前代码可作为 GitHub 源码版本；在标记桌面正式发行版之前，仍需 Windows/macOS 真机与签名验收。

以后所有开发、测试、提交和发布只围绕：

```text
D:\Projects\VineFocus
```

不要从桌面 `flower`、`D:\Codex` 或历史输出目录继续开发。

## 2. v1.7.4 最终产品规则

### 2.1 一次任务完成一株

- 用户设置 `N` 轮（1～12），点击开始时冻结本次任务的时长、总轮数和累计/单轮模式。
- 累计模式每完成一轮，桌面主藤永久进度达到 `r/N`。
- 下一轮从上一轮终点继续，不回缩、不换种子、不重新从 0 开始。
- 最后一轮完成后保持完整植株；下一次新任务开始前才换株。

### 2.2 桌面藤上花期

- 两种本轮结果都会推进桌面藤长和藤上花期。
- “目标有推进”只额外触发完成庆祝，并保留为复盘指标。
- 轮次花期使用 `(r/N)^1.15`，最终轮强制为 1.0。
- 花期只揭示当前浓密度生成的花位；不得增建花位、增加枝叶或修改浓密度。
- 清爽档槽位少时，某轮可能是同一花位继续舒展；总开放量仍严格增加。
- 当前改动只针对桌面覆盖层的藤上花。主面板/看板的装饰花仍使用旧的目标推进口径。

### 2.3 环屏节奏

- 四边同步：四个区域同时按相同比例生长。
- 等时接力：四个区域依次生长，各占总时间四分之一。
- 自然接力：四个区域依次生长，按真实藤长分配时间。
- 同步模式的开花顺序跨区域交错；接力模式按生长时间排序，不提前开未出现区域。

### 2.4 浓密度边界

- 0～100 连续滑动条只控制叶、枝、卷须、普通花苞和桌面花位容量/间距。
- 尺寸、透明度、轮次花期和管理面板装饰花均为独立轴。
- 高密度按“花位 → 侧枝 → 普通花苞 → 卷须 → 直属叶”避让，跨路径四角也检查净距。

## 3. 目录与模块

```text
VineFocus/
├── assets/botanical/
│   ├── organs/                    # 四主题 × 13 器官，共 52 个正式透明组件
│   ├── moon_base.png              # 面板/看板兼容预览底图
│   ├── cherry_base.png
│   ├── wisteria_base.png
│   ├── aurora_base.png
│   └── flower_atlas.png
├── previews/                      # 离屏视觉验收图，不参与运行
├── tests/test_smoke.py            # 逻辑、渲染、状态、窗口回归
├── tools/render_release_previews.py
├── vinefocus/
│   ├── config.py                  # 场景、主题、布局、环屏节奏常量
│   ├── models.py                  # VineNode
│   ├── state.py                   # 设置、记录、任务目标、花期、快照与备份
│   ├── visual_profiles.py         # 三种构图路径
│   ├── botanical_assets.py        # PNG 资源、锚点和面板预览
│   ├── overlay.py                 # 桌面主藤、节点、花期、避让、呼吸
│   ├── panel.py                   # 计时状态机、任务冻结、结果提交和同步边界
│   ├── settings_dialog.py         # 三页设置与环屏节奏说明
│   ├── dialogs.py                 # 产出记录与成长看板
│   ├── styles.py                  # 三套 UI 令牌和 QSS
│   ├── widgets.py                 # Toast、图表、迷你计时器、标题按钮
│   ├── tray.py                    # 托盘/菜单栏
│   ├── single_instance.py         # 单实例与唤醒
│   └── platform_integration.py    # 启动项和平台能力
├── main.py
├── pyproject.toml
├── requirements.txt
├── README.md
├── CHANGELOG.md
├── IMPLEMENTATION_NOTES.md
├── 花藤专注_完整项目需求说明书.md
└── 发布前审查报告_v1.7.4.md
```

## 4. 关键实现说明

### 4.1 数据状态

`AppState` 中与当前植物有关的字段：

- `growth_chapter`：植株编号，并参与稳定随机种子。
- `growth_target_units`：当前植株冻结的分母。
- `growth_completed_units`：当前植株已提交轮数。
- `growth_effective_units`：目标推进复盘次数；不再驱动桌面花期。
- `perimeter_growth_mode`：环屏节奏。

旧版没有 `growth_target_units`。迁移会保护已经长出的植物，不把它裁短；旧株完成后，新株采用用户新设置的 1～12 轮。

### 4.2 会话冻结

`PomodoroPanel` 在任务开始时保存：

- `session_focus_seconds`
- `session_rest_seconds`
- `session_total_rounds`
- `session_cumulative_growth`

它们也写入 `active_session.json`。运行中改动相应设置只影响下一次任务，当前倒计时和成长分母不跳变。

### 4.3 两套花朵口径

必须保留这条边界：

```text
桌面覆盖层：overlay.set_bloom_progress(r/N 曲线)
管理面板预览：BotanicalPreview.set_visual(..., growth_effective_units)
```

`sync_botanical_previews()` 故意不读取 `overlay.open_flower_count`。不要为了“看起来一致”把它改回去；是否统一管理面板花朵需要新的产品决策。

### 4.4 浓密度与花期

`build_natural_nodes()` 先按浓密度建立容量。`set_bloom_progress()` 只计算每个既有花位的 0～1 开放度。调整浓密度时覆盖层重建容量，但保留相同花期百分比。

### 4.5 绘制层级

```text
侧枝/卷须 → 连续主藤封住根部 → 端叶/直属叶/花苞/藤上花 → 自然收梢 → 落瓣/微光
```

普通主题花位以单朵或轻量双朵为主，避免终态形成重复花团；紫藤每个花位仍绘制一串花序。

## 5. 本地运行

Windows PowerShell：

```powershell
cd D:\Projects\VineFocus
python -m pip install -r requirements.txt
python main.py
```

macOS：

```bash
cd /path/to/VineFocus
python3 -m pip install -r requirements.txt
python3 main.py
```

如果运行后没有新窗口，先从旧版本托盘菜单选择“退出软件”。所有版本共享单实例标识。

## 6. 验证流程

```powershell
cd D:\Projects\VineFocus
python -m compileall -q main.py vinefocus tests tools
python -m unittest discover -s tests -v
python -m pip check
python tools\render_release_previews.py
git status --short
```

v1.7.4 自动测试共 43 项，覆盖：

- 4/8 轮逐轮花期与终轮全开。
- 浓密度容量与花期正交。
- 三种环屏节奏。
- 两种结果均推进桌面花期，面板装饰花不被改写。
- 下一轮连续生长、参数冻结、暂停/快照、新株切换。
- 四主题高密度器官避让和极光叶片方向。
- 休息独立计时与像素级呼吸差异。
- 标题按钮、主面板高度、托盘对比度、单实例和窗口恢复。

离屏验收图：

- `previews/VineFocus_v174_four_round_bloom.png`
- `previews/VineFocus_v174_density_comparison.png`
- `previews/VineFocus_v174_perimeter_timing.png`
- `previews/VineFocus_v174_all_themes_full.png`
- `previews/VineFocus_v174_glass_panel.png`
- `previews/VineFocus_v174_settings.png`
- 四套 `VineFocus_v174_*_side.png`

## 7. Git 与 GitHub

标准流程：

```powershell
cd D:\Projects\VineFocus
git status
git add <本次确认的文件>
git commit -m "release: VineFocus v1.7.4 round-based bloom"
git push origin main
```

不得提交 `.venv`、`__pycache__`、用户设置、会话快照或本地记录。

### 7.1 不满意时能否回退

可以。每次上传形成独立提交，历史版本不会消失。

- 只查看旧版：`git log --oneline`，再用 `git switch --detach <提交号>`。
- 公开分支安全撤销：`git revert <不满意的提交号>`，再正常 push；推荐用于已经推送的版本。
- 仅本地临时比较：可新建分支 `git switch -c compare-old <提交号>`。
- 不要在未备份工作时使用 `git reset --hard`，也不要对共享 `main` 强制推送。

v1.7.3 的 GitHub 基线提交为 `7e06dee344d86b88814ddaffc0090ed246b3bb39`。v1.7.4 上传后可随时用 `git revert` 撤销 v1.7.4，而不丢失历史。

## 8. 打包与发布

Windows：

```powershell
pyinstaller --noconsole --onedir --name VineFocus --add-data "assets;assets" main.py
pyinstaller --noconsole --onefile --name VineFocus --add-data "assets;assets" main.py
```

macOS：

```bash
pyinstaller --windowed --onedir --name VineFocus --add-data "assets:assets" main.py
```

Windows 需要可信代码签名；macOS 必须在真实 Mac 上构建、签名、公证并验证 Gatekeeper。

## 9. 仍需真机完成

- Windows 100%～200% DPI、双屏拔插、任务栏位置、睡眠/锁屏。
- 托盘隐藏/恢复 50 次和全屏应用隐藏/恢复。
- 4/8 轮真实计时与三种环屏动画。
- 四主题 × 三布局 × 三档浓密度在明暗桌面上的连接和遮挡。
- macOS Apple Silicon、Retina/外接屏、菜单栏、Space、Command+Q、登录项与通知。
- Windows/macOS 安装包、签名和公证。

## 10. 明确未实现

- 背景亮度自动适配：尚未读取屏幕或壁纸，不应在宣传中写为已有功能。
- 管理面板装饰花的轮次化：本版本明确不处理。
- 账号、云同步和跨设备同步。
- 原生 Acrylic/Vibrancy 实时桌面模糊。

这些内容如要开发，应先更新需求说明书和验收标准，再修改代码。
