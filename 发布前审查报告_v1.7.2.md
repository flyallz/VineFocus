# VineFocus v1.7.2 发布前审查报告

> 审查日期：2026-09-13<br>
> 审查范围：当前源码、产品需求说明书、交接文档、自动化回归与离屏启动<br>
> 结论：**源码可作为 GitHub 版本基线；桌面正式发行仍需完成 Windows 与 macOS 真机验收。**

## 1. 审查结论

产品闭环与文档描述一致：用户设置任务、完成专注、明确确认本轮结果、进入休息、看到枝叶/开花变化，并可在成长看板和本地 Markdown/JSON 中回顾记录。

这次审查没有发现会导致本地记录丢失、重复提交成长或花朵继续集中在单一区域的发布阻断问题。发现的三项逻辑歧义已在 v1.7.2 修正。

| 项目 | 审查结果 |
|---|---|
| 专注 → 记录 → 生长 → 休息 | 通过；记录窗口关闭会保留“待记录”状态，不会绕过记录 |
| 枝叶与开花因果 | 通过；每轮都生长枝叶，只有用户明确标为“目标有推进”才推进开花 |
| 结果选择 | 已修正；默认不预选，必须由用户明确选择 |
| 环屏开花 | 通过；前 4 个花位分布到 4 段路径 |
| 休息动效 | 通过；独立低频计时器驱动，减少动效时停止 |
| 成果数据 | 已修正；可选、默认折叠、不会影响成长，并汇总所有填写过的记录 |
| 数据边界 | 已修正；旧设置的非法成长计数会被限制为合理范围 |
| 隐私 | 通过；不读取屏幕、键盘或应用使用数据 |
| Windows / macOS 真机 | 待完成；当前环境只能做离屏与代码级验证 |

## 2. 本次修正与原因

### 2.1 不再默认认定“目标有推进”

此前“目标有推进”处于默认选中状态，用户可能只是想保存笔记或结束一轮，却在没有明确确认的情况下推动开花。这和“开花代表用户认可的推进”不一致。

现在产出窗口会先给出两张同等权重的选择卡：

- **本轮目标有推进**：保存记录、枝叶生长，并促进自然开花。
- **只是保持了专注**：保存记录并让枝叶生长，但不推进开花。

两者都不是失败，也不需要填写可选成果数据。用户选择其一后，才可以保存并进入休息。

### 2.2 成果数据不再与开花混在一起

字数、页数、题数等只是用户选择记录的工作量。它们不影响植物成长，也不应只因本轮未标“有推进”就从统计中消失。看板现在对所有保存记录汇总这些可选数字；“目标有推进”仍只用于展示推进次数和自然开花阶段。

### 2.3 旧数据的成长不变量

设置文件可能来自早期版本或被手动编辑。读取时已限定：

- 当前植株完成轮次为 0～24；
- 本株目标推进次数不会超过完成轮次；
- 植株编号至少为 1。

这保证“当前植物”“成长进度”和覆盖层始终表达一个可能发生的状态。

## 3. 已执行验证

| 验证 | 结果 |
|---|---|
| Python 编译检查 | 通过 |
| 无界面回归测试 | 24 / 24 通过 |
| Python 依赖一致性检查 | 通过 |
| 离屏启动：覆盖层、主面板、托盘初始化 | 通过 |
| 资产体积检查 | 69 个 PNG，合计约 12.54 MiB；没有 GitHub 100 MiB 单文件风险 |

代码风格检查未作为发布判定项：项目已配置 Ruff 规则，但当前运行环境没有安装 Ruff。它不影响运行与测试；后续开发环境可执行 `python -m pip install ruff` 后运行 `python -m ruff check .`。

## 4. 需求说明书一致性复核

- 成长看板保留为“回顾专注行为、当前植物与产出”的入口，不是额外的养成数值系统。
- “待开花苞”“解锁主题花簇”“数量决定开花”等旧表达均未保留为用户功能。
- 密度滑动条只影响枝叶/花位数量，不偷偷改变尺寸或透明度。
- 静谧单株、侧边攀援、环屏生长都遵循贴边、留白和自然收梢要求。
- 背景亮度自动适配尚未实现，也未被标记为本次交付能力。真实读取桌面像素会触及 macOS 屏幕录制权限，因此它仅列为需用户明确授权的后续项；当前版本保持不读取屏幕内容的隐私边界。

## 5. 正式发行前仍需完成

这些项目不阻碍把源码上传 GitHub，但阻碍把应用标为 Windows/macOS 正式发行版：

1. Windows 10/11 真机：100%～200% 缩放、多屏拔插、托盘恢复 50 次、独占全屏隐藏/恢复、睡眠恢复。
2. macOS 真机：菜单栏、多个 Space、全屏应用、Retina/外接屏、登录项、通知权限、Command+Q。
3. 打包与签名：Windows 代码签名；macOS `.app` 签名、公证和 Gatekeeper 验证。
4. 可选背景亮度自适配如要立项，先单独确定权限说明、开关默认值和 Windows/macOS 的降级策略。

## 6. GitHub 基线建议

- 将当前目录作为仓库根目录；提交源码、52 个植物器官、4 个兼容底图、预览图、测试和文档。
- 不提交 `.venv`、`__pycache__`、本机记录、会话快照或用户设置；`.gitignore` 已覆盖。
- 首个提交建议信息：`release: VineFocus v1.7.2 user-first growth baseline`。
- 建议先建为私有仓库；在完成双平台真机验收、签名和许可证选择后再决定是否公开。
