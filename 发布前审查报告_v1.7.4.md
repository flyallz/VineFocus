# VineFocus v1.7.4 发布前审查报告

> 审查日期：2026-09-15  
> 审查范围：源码、需求说明书、交接文档、自动回归、离屏视觉对照与数据兼容  
> 结论：**核心逻辑和离屏视觉已达到 GitHub 源码发布标准；桌面正式发行仍需 Windows/macOS 真机验收与签名。**

## 1. 这次审查解决了什么

用户反馈的表象是“花只开在左上角、下一轮没有继续长、茂盛与开花容易混在一起”。根因并非单一绘图错误，而是旧版同时存在三套不一致分母：任务使用 1～12 轮，植物固定使用 24 单元，花朵又只读取“目标有推进”次数。

v1.7.4 将逻辑拆成彼此独立的轴：

```text
任务轮数 N ──> 桌面藤长 r/N ──> 桌面藤上花期
浓密度 ──────> 叶/枝/苞/花位容量
本轮结果 ────> 复盘标记；“有推进”额外庆祝
管理面板花朵 ─> 保留既有独立口径，本版不改
```

## 2. 审查结论表

| 项目 | 结果 |
|---|---|
| 4/8 轮成长 | 通过；每轮从上一轮终点续长，终轮 100% |
| 逐轮桌面花期 | 通过；每轮总开放量严格增加，终轮开放当前全部花位 |
| 两种结果 | 通过；均推进桌面藤长/花期，“有推进”只额外庆祝和记录 |
| 浓密度正交 | 通过；决定容量，不修改花期、尺寸、透明度或管理面板花朵 |
| 环屏分布 | 通过；同步模式交错跨区域，接力模式不提前开未生长区域 |
| 环屏节奏 | 通过；四边同步、等时接力、自然接力均可设置和保存 |
| 下一轮连续性 | 通过；新计时从已提交比例继续，不回缩、不换形 |
| 任务设置稳定性 | 通过；开始后时长、轮数和累计模式冻结 |
| 高密度重叠 | 通过；四主题三布局采用优先级和跨路径坐标避让 |
| 终态花团 | 已收敛；普通主题以单朵/轻量双朵为主，紫藤保留花序 |
| 休息呼吸 | 通过；独立定时器及像素级差异验证，减少动效时停止 |
| 标题按钮 | 通过；最小化/关闭使用同尺寸、线宽、光学中心 |
| 管理面板边界 | 通过；桌面花期未改写面板装饰花数量 |
| 当前植物分母 | 通过；面板/看板读取冻结目标，不再固定 `/24` |
| 旧数据 | 通过；旧 24 单元植物不倒退，新株采用新任务轮数 |
| 隐私 | 通过；不读取屏幕、键盘或应用内容 |
| Windows/macOS 真机 | 待完成 |

## 3. 核心规则合理性复核

### 3.1 为什么不再用固定 24 轮

四轮任务若每轮只增加 `1/24`，单轮变化约 4.2%，用户很难看见，任务完成时植物也远未完成。这违反“本次任务有明确完成感”。现在任务开始时冻结 `N`，每轮长度直接到 `r/N`。

### 3.2 为什么两种结果都开花

用户已经完成整轮专注，若因为诚实选择“只是保持了专注”而阻断花期，会把复盘按钮变成奖励开关，诱导不准确记录。v1.7.4 将它改为复盘标签：两种结果都成长；“目标有推进”保留局部庆祝和统计价值。

### 3.3 为什么不是“每轮固定加 X 朵”

固定加朵数会与清爽/标准/茂盛冲突：低密度可能没有足够槽位，高密度又显得变化不足。现在先由浓密度生成容量，再用连续花期揭示。低密度某轮可表现为一朵继续舒展，高密度同一比例会自然影响更多花位，终轮都只开放各自已有容量。

### 3.4 为什么最后一轮全开不会破坏浓密度

“全开”只把当前花位的开放度设为 1，不创建新节点。自动测试在花期切换前后逐项比较叶、枝、卷须、普通花苞和花位数量，确认完全不变。

### 3.5 为什么管理面板花朵暂时不跟随

用户明确限定当前讨论的是桌面藤蔓上的花。v1.7.4 只同步面板文字进度和真实分母，管理面板小植物的花仍读取历史 `growth_effective_units`。这是有意隔离，不是漏接。

## 4. 环屏与构图复核

### 4.1 三种时间分配

- 四边同步：每段可见长度均为“本段长度 × 总进度”。
- 等时接力：进度轴四等分，按段依次完成。
- 自然接力：连续消耗真实路径长度，较长路径自然获得更多时间。

三种模式最终节点、终态容量和花期一致，只改变出现顺序。

### 4.2 区域分布

同步模式的花位采用稳定随机的区域交错顺序，不会先塞满第一角；接力模式按时间距离排序，保证花只出现在已经长成的段上。屏幕重绘、暂停和重启不会改变同一株的种子。

### 4.3 重叠与收梢

- 极光心形直属叶固定朝向内容区，不生成共锚反向副本。
- 花位优先，其他器官按层级让位；不同路径在四角也按实际坐标检查。
- 侧枝为连续曲线，主藤覆盖根部接缝。
- 完成路径以嫩叶/花苞封住端点，不暴露裁切圆头。

## 5. UI 与状态机复核

- 任务开始时冻结专注秒数、休息秒数、总轮数和累计模式；设置变化留给下一任务。
- 暂停保存精确进度，继续不重复换株。
- 已提交轮次永久保留；重置只放弃尚未提交的当前计时。
- 记录成功后再增长；记录窗口关闭仍停留“待记录”。
- 休息与下一轮都不会自动开始。
- 最小化与关闭不再使用不同字体字形，而是自绘同规格图标。
- 主面板继续按内容高度收敛，避免底部大块空白。

## 6. 已执行验证

| 验证 | 结果 |
|---|---|
| Python 全量编译 | 通过 |
| 无界面自动回归 | 43 / 43 通过 |
| 依赖一致性 `pip check` | 通过 |
| 四主题 × 三布局离屏渲染 | 通过 |
| 4 轮花期对照图 | 通过 |
| 三档浓密度对照图 | 通过 |
| 三种环屏节奏对照图 | 通过 |
| 四主题茂盛终态对照图 | 通过并完成一次花团收敛迭代 |
| 主面板离屏渲染 | 通过 |
| 外观设置页结构渲染 | 通过；中文字体留待 Windows 真机确认 |

当前 Python 环境未安装 Ruff，因此没有执行 Ruff；项目已保留 Ruff 配置。此项不影响运行，但后续开发环境可安装后执行 `python -m ruff check .`。

主要验收图位于：

- `previews/VineFocus_v174_four_round_bloom.png`
- `previews/VineFocus_v174_density_comparison.png`
- `previews/VineFocus_v174_perimeter_timing.png`
- `previews/VineFocus_v174_all_themes_full.png`
- `previews/VineFocus_v174_glass_panel.png`
- `previews/VineFocus_v174_settings.png`

## 7. 需求说明书一致性

- PRD 已明确任务轮数、桌面藤上花期、浓密度、结果标记和管理面板装饰花的五条边界。
- “待开花苞”“解锁主题花簇”“填写数量决定开花”等旧产品设定未重新引入。
- 看板保持回顾工具定位，不成为第二个计时器或隐性养成系统。
- 背景亮度适配仍为后续候选，没有伪装成已实现能力。

## 8. 正式发行前待办

1. Windows 10/11：100%～200% DPI、不同任务栏位置、双屏拔插、睡眠恢复。
2. Windows：托盘隐藏/恢复 50 次、独占全屏隐藏/恢复、真实通知和启动项。
3. macOS：Apple Silicon、Retina/外接屏、菜单栏、多个 Space、Command+Q、通知和登录项。
4. 真实桌面完成 4/8 轮与三种环屏节奏的连续动画录像复核。
5. Windows 代码签名；macOS 构建、签名、公证和 Gatekeeper 验证。

## 9. GitHub 与回退

本次建议提交信息：

```text
release: VineFocus v1.7.4 round-based bloom
```

已推送版本可以回退。公开 `main` 推荐使用 `git revert <commit>` 生成一条安全撤销提交，不改写已有历史；不要在未备份时执行 `git reset --hard` 或对共享分支强制推送。
