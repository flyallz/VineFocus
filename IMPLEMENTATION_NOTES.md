# VineFocus v1.7.3 四边花簇与界面收敛实现说明

## 已完成

- 主面板按“夜色流光”方向重排：计时优先、任务摘要默认收起、今日生长降为辅助信息。
- 设置页包含 3 套界面、4 套植物、3 种布局、显示器、存在感、透明度、减少动效、玻璃效果、通知、声音、启动与记录目录。
- 月白花藤、樱雾花枝、流苏紫藤、极光荧藤均采用独立路径和器官参数；紫藤花序使用不对称长短与错位，避免整齐垂线。
- 植物主体使用 52 个正式透明器官：每个主题各含 3 类枝条、4 级叶龄、卷须、2 级花苞、半开、盛开和落瓣；QPainter 只承担连续路径、节点年龄、变换、成长尖端、呼吸和微光。
- 侧枝改为连续曲线骨架，木质组件降为小尺寸纹理；绘制顺序固定为“侧枝与卷须 → 主藤封口 → 端叶与花苞/花朵”，不再暴露圆形切口。
- 普通直属叶片使用稳定的不规则分布；极光直属叶全部朝向内容区并取消同锚点反向叶，连接净距提高到 30px，避免心形叶、枝根和花苞相互堆叠。
- 4K/Retina 的器官缩放上限为 1.35，不随像素尺寸无限放大；高 DPI 下枝条仍保持细而连续。
- 52 个器官不再共用底部中央锚点：普通叶、木质枝、花梗、紫藤复叶/花序以及极光心形叶分别使用真实连接位置和局部生长轴。
- 每段已完成路径会补绘端叶收梢；连续密度值映射到 0.48～1.70 的节点系数，但不再改变器官尺寸和透明度。
- 侧边攀援改为固定逻辑像素边缘带，在 4K/超宽屏上主藤也保持贴边，只有枝叶向内容区自然伸展。
- 开花采用植物阶段而非面向用户的精确朵数：月白、樱雾、极光按习性形成多花组合，紫藤形成完整花序；“花簇”仅是内部绘制结构。
- 夜色主题降低青色饱和度和边框亮度，外壳、计时卡、普通卡片分成三层半透明表面，并以跨平台柔和阴影补充景深。
- 正式模式不再裁切或淡入整株完成图；侧枝先长、叶片四级舒展，花位先闭合再膨大，目标有推进时继续半开和盛开。
- 修正高清植物资源自身透明留白造成的假性内缩：按每套植物的真实 Alpha 内容边界裁切，以完整屏幕几何定位，并让叶尖轻微越界贴边。
- 环屏构图由四角等大镜像改为概念稿式不对称平衡，每套植物分别安排主景、次景与低透明点缀，保留中央工作区。
- 设置外观页使用三套界面卡、四套真实植物预览卡和三段式构图选择；首次启动向导复用正式设置值。
- 成长看板升级为植物主视觉、环形进度、四项关键数据、柱线趋势、内容分布和最近产出卡。
- 迷你计时器默认收拢为 52px 植物圆点，悬停展开；托盘恢复额外重建激活状态并请求系统激活窗口。
- 默认使用静谧单株；侧边攀援和带断点的环屏生长可即时切换。
- 每轮专注都推动成长；记录成功且标记为“目标有推进”后推进开花阶段。仅保持专注仍保存记录并继续生长枝叶。
- 环屏四段不再串行消耗成长进度，而是共享同一条 0～100% 时间轴；稳定随机轮转保证前四次目标推进分布在四段不同区域。
- 每个推进事件对应一个内部开花位置：月白/樱雾/极光从同一根部舒展 3～4 朵，紫藤展开一串完整花序；新花约 1.2 秒从苞到盛开，未获得花位不显示为“待开花苞”。
- 休息呼吸由 80ms 低频计时器维持，花朵约 5.5% 缩放、枝叶约 2° 轻摆；减少动效时全部停止。
- 主面板以内容高度为唯一高度，任务编辑开合时重新计算；卡片植物预览取消 55%/68% 最低成熟度，与覆盖层实时同步。
- 托盘菜单脱离主面板样式继承，Windows/Linux 显式设置主题前景与背景，macOS 保留原生菜单。
- 满 24 轮时在下一次计时开始前完成换株和随机种子切换，避免提交记录后植物形态突然跳变。
- 看板植物使用当前植株的 24 轮章节状态，时间范围只控制历史统计；删除“待开花苞”和面向用户的“花簇数量”。
- 成果数量默认折叠为可选高级字段，不参与成长和开花判断。
- 产出记录不再预选“目标有推进”：用户须明确选择本轮结果，避免意外推进开花；看板会汇总全部已填写的可选成果数据。
- 专注结束必须处理记录；关闭记录窗后停留在“待记录”。休息结束只进入下一轮准备，不自动开始。
- 支持最后一轮跳过休息、休息阶段手动跳过、活跃状态重置确认、会话快照和休眠后自动暂停。
- 主面板、设置、产出记录、看板、迷你计时器与托盘/菜单栏均为单实例；第二次启动只唤醒现有进程。
- 修复托盘恢复顺序与屏外窗口回收；回归测试连续隐藏/恢复 25 次，并保留 50 次真机验收要求。
- 支持跟随面板、主显示器或具体显示器；外接屏断开时迁回主屏。
- Windows 全屏应用前台运行时暂时隐藏植物；macOS 不申请屏幕录制权限。
- 本地 JSON 使用原子替换和 3 份滚动备份；自定义记录目录先验证可写。

## 自动验证结果

- Python 全量编译通过。
- 32 项无界面测试通过：新增首轮可见花簇、四区域像素级成像、“只是保持专注”端到端成长、极光叶朝向与去重、托盘菜单对比度、面板自适应高度、真实卡片进度、暂停冻结和换株时机回归；并继续覆盖四植物 × 三布局渲染、稳定随机、成长、单实例与窗口恢复。
- 应用入口完成离屏启动检查，没有 Python 异常。
- 当前环境未安装 Ruff，因此未执行 Ruff 风格检查；这不影响编译和运行。

## 仍需真机确认

- Windows：不同 DPI、任务栏位置、双屏拔插、真实托盘连续恢复 50 次、睡眠/锁屏、系统通知和全屏应用。
- macOS：必须在 Mac 上验证 Apple Silicon、Retina/外接屏、菜单栏、多个 Space、Command+Q、登录项与通知权限。
- 对外分发前完成 Windows 代码签名，以及 macOS 签名与公证。

## 数据兼容

原开发目录仍兼容 `~/.vine_pomodoro` 的旧设置和记录。主题旧名称会迁移到新名称，不移动、不删除历史数据。
