# VineFocus v1.7.4 轮次花期实现说明

## 核心改动

- 当前植物不再固定使用 24 轮。任务开始时把用户设置的 1～12 轮冻结为 `growth_target_units`，桌面藤长按 `completed / target` 计算。
- 专注时长、休息时长、总轮数和累计/单轮模式写入会话计划及快照，运行中修改设置不会改变当前任务。
- “目标有推进”和“只是保持了专注”都会推进桌面藤长与藤上花期；前者只增加复盘计数并触发局部庆祝。
- 桌面花期使用 `(r/N)^1.15`，最后一轮强制为 1.0。每个花位用连续开放度从苞到半开再到盛开。
- 花期百分比只映射到 `flower_nodes`，不调用节点生成，不改变浓密度。
- 主面板和看板小植物继续以 `growth_effective_units` 作为装饰花朵口径；仅文字进度与分母同步当前植物目标。

## 环屏节奏

- `四边同步`：每段使用相同局部进度。
- `等时接力`：总进度四等分后依次映射到四段。
- `自然接力`：按真实累计路径长度依次映射。
- 同步模式花位按稳定随机的跨区域轮转排序；两种接力模式按时间距离排序。
- 设置页只在“环屏生长”下启用选择框，并显示当前模式的通俗说明。

## 密度与构图

- 0～100 浓密度先生成叶、枝、卷须、普通花苞和花位容量。
- 普通主题的桌面花位由单朵或轻量双朵组成，避免茂盛终态形成重复花团；紫藤保留单个完整花序。
- 花位、侧枝、普通花苞、卷须、直属叶按优先级过滤；不同路径在四角也检查实际坐标距离。
- 极光心形直属叶只朝屏幕内容区；同锚点反向叶仍禁止。
- 侧枝使用连续曲线，主藤覆盖枝根接缝，完成端点使用嫩叶/花苞收梢。

## 休息与界面

- 休息动画由 80ms 独立计时器维持，不依赖休息倒计时是否运行。
- 除叶片轻摆和花朵局部呼吸外，整株增加约 ±3% 透明度、±1.8% 尺寸呼吸；减少动效时全部停止。
- 标题栏最小化和关闭按钮由 `WindowControlButton` 在同一 34×30 画布上自绘，使用相同线宽与光学中心。
- 主面板继续按可见内容高度收敛。

## 数据兼容

- 新字段：`growth_target_units`、`perimeter_growth_mode`。
- 会话快照新增：`session_focus_seconds`、`session_rest_seconds`、`session_total_rounds`、`session_cumulative_growth`。
- 旧版固定 24 轮数据迁移时不会把已长出的植物裁短；完成旧株后，新株采用当前用户轮数。
- `growth_effective_units` 和记录字段 `effective` 保留，以兼容历史复盘和管理面板装饰花。

## 自动验证

- Python 全量编译通过。
- 43 项无界面回归通过，新增覆盖 4/8 轮花期、终轮全开、浓密度正交、枝叶层级、三种环屏节奏、两种结果、下一轮续长、累计/单轮模式切换、任务参数冻结、高密度净距、管理面板边界、标题按钮和像素级休息呼吸。
- `pip check` 通过。
- 已生成 4 轮、三档浓密度、三种环屏节奏、四主题茂盛终态、四主题侧边和主面板验收图。
- 当前环境未安装 Ruff，未执行 Ruff 风格检查。

## 尚需真机验证

- Windows 10/11 不同 DPI、任务栏位置、双屏拔插、睡眠恢复、通知、启动项和托盘恢复 50 次。
- macOS Apple Silicon、Retina、外接屏、菜单栏、多个 Space、Command+Q、登录项与通知。
- 4/8 轮真实计时及三种环屏连续动画。
- Windows 签名和 macOS 构建、签名、公证。

## 明确未实现

- 背景亮度自动适配。
- 管理面板装饰花的轮次化。
- 原生桌面实时模糊、账号和云同步。
